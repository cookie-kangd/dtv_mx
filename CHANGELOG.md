# dtv_mx 更新日志

## v0.2.10

### 修复问题
- **顶栏下拉菜单打开时自动定位到当前选中项**：选项较多的平台（如海外平台分类多达 100 个）此前每次打开菜单都要从头滑到选中项，现在打开后直接停在当前选中项附近，上下选项也保留可见。
- **分类胶囊条滚动位置恢复修正**：v0.2.9 的位置记忆在部分场景仍会失效——返回页面时分类列表异步填充的瞬间，空列表会把滚动位置错误地覆盖成开头。现在恢复动作等列表就绪后执行一次，且列表为空时绝不回写，彻底保证「滚到哪、回来就在哪」。
- **海外平台搜索结果不全**：搜索接口固定只返回 10 条按相关度排序的结果，搜具体频道名时经常排不进去。现在并发追加一次精确频道直查，命中后置顶合并，直接搜频道名必出结果。

## v0.2.9

### 修复问题
- **分类选择被重置**：进直播间时平台页会整体销毁，返回重建后还没读回上次的分类就先把「默认分类」写了回去，导致选中的分类被清掉。现改为恢复完成后再落库，看完直播返回仍停在原分类。
- **分类胶囊条滚回最前**：同上的销毁重建问题，现在按「平台 + 分类」记住胶囊条的横向滚动位置，返回后停在离开前的位置。
- **退出清理缓存更可靠**：原来只依赖「切后台 2 秒后」一条路径，划掉卡片、系统直接杀进程时根本没机会执行。现在补了三条路径——返回键退出立即清理、切后台延迟清理（短暂切回不误清）、异常退出在下一次启动时补清；同时把图片内存缓存也一并清掉。

### 优化
- 海外平台分类数量扩充到接口上限（100 个，此前只有 40 个），与网页版分类规模一致。

## v0.2.8

### 修复问题
- **搜索输入体验重构**：去掉逐键联想搜索，输入停顿约半秒或按输入法上的「搜索」键后自动搜索一次；搜索全程不关闭输入法、不清空已输入内容，结果只在下方下拉面板里刷新。修复部分机型连打两个字符只进一个、输入法弹窗自动消失的问题。
- **安卓 12 以下底栏样式异常**：旧系统不支持实时模糊，毛玻璃底栏会直接透出滚动内容导致文字难读。现自动降级为接近毛玻璃观感的高不透明度实色底，各系统版本显示一致。
- **安卓 10 以下检查更新下载失败（Permission denied）**：旧系统写公共下载目录需要存储权限，现改为下载到应用专属目录，无需任何权限即可完成下载与安装，全系统版本可用。

### 优化
- 更新提示文案更准确（安装包缓存位置随系统自适应）。

## v0.2.7

### 修复问题
- **搜索框输入丢字符 / 输入法自动收起**：平台页搜索框输入第一个字符后，联想面板会抢走输入框焦点，导致输入法自动收起、后续输入的字符全部丢失（例如连打两个 y 只进去一个）。现联想面板改为不获取焦点，输入框全程保持输入状态，连续输入不再丢字。
- **平板进直播间默认横屏**：平板系统本身就是横屏，此前进直播间会跟随系统方向直接进入横屏全屏。现进直播间只跟随「默认横屏」开关——未开启时一律竖屏进入（横屏设备也会锁定竖屏），开启后才横屏进入。
- **开启「默认横屏」后需要退出两次才回竖屏**：优化退出方向控制，横屏进入后退出一次即回到竖屏（再按一次才是关闭直播间）。

### 体验优化
- 搜索联想面板：按返回键优先收起面板（不再直接离开当前页）；点击面板外区域自动收起；清空搜索时同步收起输入法。

## v0.2.6

### 修复问题
- **横屏设置面板可读性彻底修复**：横屏播放时打开设置面板，面板改为固定不透明深色底，不再受背后播放画面明暗影响，任何场景下选项文字都清晰可读。

### 性能与续航
- **背景渐变绘制缓存**：全局背景的多层渐变改为按需缓存绘制，仅在主题或尺寸变化时重建画笔，滚动与切页时减少每帧对象分配。

### 界面
- 设置面板在实心深色底上保留一层极淡的纵向明暗渐变，层次感更自然。
- **分类入口优化**：分类较多的平台隐藏第二行横向分类条（滑动翻找不便），全部分类收纳进顶栏右侧下拉菜单——菜单限高后内部滚动、长分类名省略号显示，并新增「推荐」入口；其余平台分类交互不变，菜单观感同步提升。

## v0.2.5

### 修复问题
- **横屏设置面板看不清**：横屏播放时打开设置面板，毛玻璃底色过透导致选项文字被背后的视频画面冲淡。现加深面板底色并收紧模糊半径，保留玻璃质感的同时文字清晰可读。
- **平台排序缺少已关闭的平台**：「平台设置 → 平台排序」此前只显示已启用的平台，被关闭的平台在排序列表里「消失」。现排序列表展示全部平台（已关闭的置灰并标注），关闭中的平台同样可拖动位置，重新开启后按此顺序加回底栏。

### 界面
- **分类胶囊按压反馈**：平台页分类胶囊按下时轻微缩小、松开回弹，与直播间卡片手感一致（空闲零动画开销）。

### 平台设置
- 平台启用与排序逻辑统一：全部平台始终出现在排序列表中，关闭某平台仅将其从底栏隐藏，不再影响排序管理。

## v0.2.4

### 界面
- **卡片按压反馈**：首页与平台页的直播间卡片按下时轻微缩小、松开回弹，触感更清晰（去掉了突兀的方块水波纹，空闲时零动画开销）。

### 性能与续航
- **日志异步落盘**：应用内日志改为内存队列 + 后台线程批量写入，弹幕高频收发、图片加载等热路径不再执行同步磁盘 IO，直播时更流畅、更省电（崩溃记录仍会先强制刷盘，日志完整性不受影响）。
- **网格卡片解析复用**：平台页卡片的封面/头像地址解析按输入记忆，长列表滚动重组不再重复正则解析。

### 其他
- 平台设置文案更新，明确各平台默认开启状态与开启方式。

## v0.2.3

### 界面
- **屏幕高刷**：「屏幕刷新率」更名为「屏幕高刷」，功能不变。
- **板块按钮瘦身**：顶栏板块下拉按钮从 108dp 收窄到 90dp（文字区仍保证最长 4 个字的板块名完整显示），搜索框空间更大。
- **播放器设置面板毛玻璃化**：横屏播放时的设置抽屉升级为真·毛玻璃——背后的视频画面被实时模糊透出，配合玻璃高光渐变与亮描边，与底部毛玻璃浮岛观感一致；低版本系统自动降级为原半透明面板。
- **板块下拉菜单毛玻璃质感**：半透明底 + 玻璃高光 + 亮描边，与整体毛玻璃风格统一。

### 性能优化
- **图片请求复用**：封面/头像的图片请求按 URL 记忆，长列表滚动重组时不再重复构建请求对象与解析 URL，滚动更省 CPU。
- **卡片资源解析复用**：首页卡片的封面/头像地址解析按输入记忆，减少重复正则解析开销。

## v0.2.2

### 系统要求
- **安装门槛降低**：最低系统版本从 Android 8.0 降至 **Android 7.0**，更多老设备可以安装使用（高版本设备完全不受影响）。

### 性能优化
- **全局图片缓存**：封面/头像启用统一图片加载器——内存缓存占可用内存 20%，磁盘缓存 256MB；忽略平台 CDN 的 no-store 响应头，切分区、搜索来回滚动时封面直接命中本地缓存，不再重复下载，长列表滚动明显更流畅。
- **列表复用**：加载骨架占位项声明 contentType，与真实卡片区分开，加载态切换时组合复用更高效。

### 界面
- **关注图标统一红**：直播间卡片、搜索结果上的已关注图标改为与播放器内一致的玫红色（不再跟随全局主题色变化），视觉语义更统一。
- **仓库瘦身**：移除未使用的截图等静态资源。

## v0.2.1

### 界面与交互
- **顶栏更简洁、搜索框更长**：平台页顶栏不再重复显示左侧平台名（毛玻璃底栏已标明当前平台），搜索框吃到整行剩余宽度；板块下拉按钮加宽到 108dp，最长 4 个字的板块名必然完整显示，不再出现省略号。
- **搜索框毛玻璃质感**：半透明底 + 发丝描边，暗色主题下用白色高光描边模拟玻璃边缘。

### 新功能
- **屏幕刷新率设置**：设置 → 基本设置 新增「屏幕刷新率」开关（默认开启）。开启 = 跟随系统最高刷新率（高刷屏 90/120/144Hz 生效，滑动与动画更流畅）；关闭 = 锁定 60Hz 更省电。切换立即生效并持久保存。

### 性能优化
- **封面图片解码**：封面/头像启用 RGB_565 解码，内存占用减半，长列表滚动 GC 压力显著下降；crossfade 缩短到 120ms。
- **弹幕重绘**：滚动弹幕从每帧触发布局重排的 offset 改为 graphicsLayer 图层平移（渲染开销最低一档）；弹幕列表声明 contentType 提升组合复用。
- **播放器**：沿用 v0.2.0 的解码器回退/紧凑缓冲/8s 超时配置，本轮复查听播链路确认无回归——听播仍是「摘画面不重建播放器」的极低内存省电状态。

## v0.2.0

全新 UI 初始版本。v0.1 ~ v0.1.25 均可在应用内直接 OTA 升级到本版本。

### 界面美化
- **顶栏搜索框统一且更长**：四平台顶栏的板块下拉按钮固定为 4 个汉字宽度（98dp），不再随板块名长短变化；搜索框占据其余全部空间，输入内容一览无余，四个平台搜索框长度完全一致。
- **设置根页分组卡片化**：五个设置入口收进同一张圆角卡片，告别散落的透明行，对齐主流 App 设置页风格。
- **平台登录页重做**：每个平台一张圆角卡片（左侧平台图标+状态，卡片主体点击登录）；已登录时卡片最右侧垂直居中显示「退出登录」图标，点击即退出对应平台。结构按平台组织，为后续抖音登录预留扩展。
- **关于页排版优化**：「检查更新」整体包进圆角卡片，入口行（图标+标题+当前版本+刷新按钮）与检查结果、下载进度同卡展示，不再直接贴在页面背景上。
- **首页空状态美化**：全新空态视图（主题色图标徽章 + 主文案 + 引导副文案），引导用户去平台页关注主播。
- **细节打磨**：分类胶囊选中阴影减淡、卡片描边/底色统一收敛到公共配色组件。

### 性能与内核
- **播放器解码器回退**：个别机型硬解失败时自动回退软解继续播放，减少黑屏报错；正常机型零开销。
- **网络超时收紧**：播放直链连接/读取超时 8s，坏线路更快失败、更快走重连/换线路路径。
- **死代码清理**：删除完全未使用的 backend 接口层（DtvBackend/StubDtvBackend）、StreamerCard 搜索卡样式（Search 卡片已被内联搜索取代）、未使用的卡片规格访问器，卡片配色收敛为 DtvCardDefaults 公共组件（4 处重复硬编码归一）。

### 稳定性
- 以上改动均不触及播放器轨道策略、听播摘挂 surface 机制、弹幕合流逻辑等关键路径，功能与 v0.1.25 完全一致。

## v0.1.25

### 新功能
- **设置新增「平台登录」**：B站登录入口从平台页顶栏迁移到「设置 → 平台登录」，点进去显示需要登录的平台（当前为B站），点击弹出登录弹窗，支持二维码登录与账号密码登录；原有登录态升级后自动保留，已登录时可直接退出登录。
- **四平台外层界面完全统一**：B站登录图标移出顶栏后，四个平台的顶栏由共用组件 HubTopBar 渲染，搜索框与板块下拉按钮长度、大小、规格完全一致。

### 优化
- **卡片徽标对齐美化**：直播间卡片左上角关注徽标与右上角人气徽标改为同高（大/小卡片模式分别 24dp / 22-26dp）、同内边距、同圆角样式，上下严格对齐，观感更整齐。
- **板块下拉按钮与搜索框同规格**：统一为 44dp 高胶囊，背景始终为全局高亮色，文字与图标反白显示当前板块名。
- **下拉菜单圆角美化**：板块菜单与搜索结果面板统一改为大圆角卡片（16dp 圆角 + 描边 + 阴影），菜单项圆角高亮、间距舒适；点击面板外部或按返回键收起。
- **切换平台即释放播放器**：看直播时通过底栏直接切换平台，立即清理播放页残留状态，ExoPlayer 随播放页销毁被彻底释放（stop/release），弹幕协程与监听同步终止，不占内存。

## v0.1.24

### 新功能
- **顶栏板块下拉按钮显示当前板块名**：按钮从纯图标改为「板块名 + 下拉图标」的胶囊按钮（如「网游竞技 ▾」），当前板块一眼可见，菜单内当前项仍以全局色高亮。
- **平台页内联搜索**：四个平台顶栏的搜索框改为就地输入——输入关键字后直接在搜索框下方弹出结果面板，点击结果进直播间、点右侧爱心可关注/取关主播；不再跳转到独立搜索页（独立搜索页已移除）。搜索结果带封面缩略图、标题、主播名与人气。
- **直播间卡片封面左上角新增关注徽标**：未关注显示空心爱心、已关注显示实心爱心（全局色），点击即可关注/取关，无需先进入直播间；平台页网格、首页（关注列表）与搜索结果均支持。
- **检查更新支持「停止下载」**：下载新版本时进度条右侧出现「停止下载」按钮，点击立即取消所有下载任务，并清理 Download 目录里本应用的全部安装包缓存（含未完成的下载残留）。

### 优化
- **依赖复查**：逐一核对后本轮无可安全升级的依赖——Ktor 3 / Coil 3 / OkHttp 5 / haze 1.x 均为破坏性迁移；core-ktx 受 AGP 8.5.2 上限约束；Kotlin/Compose/AGP 升级牵动全链路，按「影响功能和稳定性就不升级」原则全部保持不动。
- 死代码清理：删除已无入口的独立搜索页（SearchScreen）与无人引用的 FollowScreen，AppState 移除对应的 Search 路由状态。

## v0.1.23

### 修复问题
- **竖屏进直播间弹幕被毛玻璃底栏遮挡**：竖屏布局下的弹幕面板此前一直延伸到屏幕底部，最新弹幕被悬浮毛玻璃浮岛底栏盖住看不清。现弹幕面板底部预留「导航条高度 + 浮岛高度」的安全距离，最后一条弹幕始终完整可见。

### 优化
- **平台首页顶栏重构**：①移除各平台右上角的「订阅」按钮与整个分区订阅功能；②右上角改为「板块下拉菜单」，可切换原来第二列的一级板块（如斗鱼「网游竞技 / 单机热游 / 手游休闲」）；③第二列一级板块移除，具体分类（英雄联盟 / CS2 等）直接平铺展示，选中项以全局主题色高亮；④移除第三列「当前：英雄联盟」文字与「更多」三点弹层。
- **四个平台首页抽出共用 UI 组件 `PlatformHomeContent`**：分类胶囊条、直播间网格、加载提示、下拉刷新等界面骨架合并为单一公共组件，斗鱼/虎牙/抖音/B站四个页面只保留各自的数据逻辑（分区模型与拉取接口各不相同）。此后界面调整只需改公共组件一处，全部平台同步生效。
- 死代码清理：删除各平台残留的底部弹层（选择分类/分区）与订阅相关代码，不影响任何现有功能。

## v0.1.22

### 新功能
- **设置新增「关于」页**（对齐热门 App 布局）：居中应用图标 + 名称 + 当前版本号，应用简介与功能一览卡片；原设置根页的「检查更新」入口移入「关于」页；底部新增「上次更新」卡片，展示上一次版本号（v0.1.21）及其更新内容；页脚显示当前完整版本。

### 修复问题
- **退出 App 不彻底、重启手机后后台列表仍出现 dtv_mx**：两个根因——①系统默认把「最近任务」持久化到磁盘并在重启后恢复，App 未声明 `persistableMode`，所以重启后卡片又回来了（进程本身已死，属系统行为）；②「熄屏听播」保活服务声明为 `START_STICKY` 且未设置任务移除处理——在听播状态下划掉 App 或进程被系统回收后，该服务会被**自动重启**，变成一个没有播放器、只挂着常驻通知的僵尸后台，进程长期驻留。现修复：MainActivity 声明 `persistableMode="persistNever"`（重启后不再出现后台卡片）；保活服务声明 `stopWithTask="true"` 并增加 `onTaskRemoved` 兜底、启动策略改为 `START_NOT_STICKY`——划掉 App / 退出 App 时保活服务立即停止、通知随之移除，且绝不会被自动重启，后台不再有任何 dtv_mx 残留。

## v0.1.21

### 修复问题
- **斗鱼听播无限加载/黑屏无声音、关闭后画面卡死（根治）**：斗鱼直链由 `getH5Play` 签名生成，带**一次性 token** 且 CDN 对同一 token 的并发/二次连接敏感。此前开关听播会**用同一 URL 重建播放器**——旧连接尚未被 CDN 释放，新连接被拒或限流 → 开启听播无限加载/黑屏无声音；重连后的流被挤占 → 关闭后画面过一会卡死（其他平台 CDN 宽容，故无此问题）。现改为**听播开关完全不重建播放器**：播放器每个直链只构建一次，开启听播仅摘掉视频输出 surface（`clearVideoSurface`，ExoPlayer 官方后台播放姿势，音频与网络连接不动），关闭听播把画面挂回（新 surface 恢复渲染）。开关即时生效、零重连、零连接竞争，对所有平台行为一致。同时听播期间允许熄屏（画面已摘除，不再常亮）。

### 优化
- **检查更新下载按钮排版**：github 原始地址与各加速镜像按钮全部等大等色（统一 OutlinedButton），并排流式排列、一行放不下自动换行，不再有独占一行的超大主按钮。

## v0.1.20

### 修复问题
- **听播关闭后视频画面卡死（彻底修复）**：v0.1.19 把「UI 层的听播切换」改成了重建播放器，但 `BackgroundAudioController` 里仍残留运行期切换视频轨的代码——关闭听播的瞬间它会对**旧播放器**执行 `setTrackTypeDisabled(false)`（临终前恢复视频渲染器，而此时旧播放器的 surface 刚被转交），部分机型上这会污染编解码器/surface 状态，导致紧接着的新播放器视频渲染器卡死（画面定格、音频与弹幕正常，需手动刷新直播间）。现彻底删除控制器内所有运行期轨道切换逻辑（**只保留前台服务保活**，视频轨开/关完全由「建播放器时定死」负责）；同时播放器重建时把 PlayerView 与 SurfaceView 一并换新（`key(player)`），新播放器拿到全新 surface，杜绝复用旧 surface 的渲染隐患。关闭听播仍会重连一次直播流（约 1-2 秒）。

### 优化
- **设置页返回层级唯一化**：此前点设置图标进入设置后，根页与子页（如「基本设置」）会同时出现两个返回入口（顶栏返回 + 页头返回 / 条目行尾小箭头）。现改为每页只有一个大返回：设置根页返回回首页，子页返回回设置根页，系统返回键行为一致；设置条目行尾的小箭头一并移除，界面更干净。
- 依赖与代码健康复查：无新增问题；未做影响稳定性的依赖升级。

## v0.1.19

### 修复问题
- **熄屏听播（耳机按钮）关闭后恢复画面过一会定格**：旧实现是在直播播放器运行期间动态开关视频轨（`setTrackTypeDisabled`），部分机型 / 直播流下视频渲染器恢复失败——音频和弹幕正常，但画面停在旧帧直到卡死。现改为**建播放器时一次性定死视频轨开关**：开启听播即按纯音频起播（无「先出画面再关」闪烁），关闭听播则**重建播放器**恢复画面。不再做运行期轨道切换，从根上规避该渲染器卡死；代价是每次开关听播会重连一次直播流（约 1-2 秒）。

### 优化
- **检查更新支持多下载源 + gh-proxy.org 加速镜像**：发现新版本时不再只有一个下载按钮——第一个是 `github` 原始地址（通栏主按钮），其余是流式排列的镜像加速按钮：`cloudflare`、`Cloudflare (v4推荐)`、`Cloudflare (v4/v6)`、`Fastly (v4)`、`AxisNow (v4)`（通过 gh-proxy.org 系列前缀转换下载地址）。镜像源在国内直连 GitHub 慢/失败时可显著加速 APK 拉取；多个来源走同一套下载/安装流程，任意一个失败可换另一个。注意：更新弹窗由**手机上已安装的 App 版本**渲染，需先安装本版本，之后的更新提示才会出现多下载源按钮。
- **README 全面润色**：对齐主流开源项目风格——居中标题与徽章、特性一览、下载加速镜像表、折叠式 FAQ、技术栈与免责声明分区。

## v0.1.18

### 修复问题
- **竖排弹幕列表重复 key 崩溃（致命）**：v0.1.17 把弹幕列表的 key 改成「消息对象本身」，但 `DanmakuMessage` 是 data class，内容完全相同的两条弹幕（如重复的"666""主播好"）会因 `equals` 撞 key，触发 LazyLayout 直接抛异常 → 直播间播放几秒到几分钟后闪退。现改为按「对象引用身份(===)」分配稳定且唯一的自增 key：同一条弹幕在缓冲区滚动/裁剪时 key 不变（不会整列重组），内容相同的不同弹幕也各自拿到不同 key，彻底消除崩溃。该崩溃在竖屏看横屏源、或画中画退出/点全屏后露出竖排弹幕面板时必现。
- **画中画 → 全屏状态机加固**：进入画中画时改为只在 `enter()` 真正提交成功后才隐藏底部栏并翻转 `playerFullscreen`，避免进入失败却残留隐藏状态；退出画中画统一复位 `playerFullscreen` 并清空残留标记，点击全屏时也会先清空该标记，杜绝旧状态叠加导致的异常。
- **设置子页面底部被毛玻璃底栏遮挡**：「平台设置-平台排序」「基本设置」「数据同步」三个子页面此前漏加底部预留高度，4 个平台时排序列表末位会被浮岛挡住无法操作。现已统一在滚动内容底部补 `DockContentClearance` 间距。

### 优化
- **毛玻璃底栏更精致**：浮岛内部 items 最小高度 54→48dp、各内边距与图标/文字间距收紧、标签字号略减，整体更紧凑不突兀（描边明显度保持不变）。

## v0.1.17

### 修复问题
- **毛玻璃底栏描边不明显**：浮岛描边加粗至 1.6dp，深色模式白色描边透明度 0.30、浅色模式 0.90，并叠加一道由上至下的玻璃高光渐变（深色 0.10→0.02、浅色 0.42→0.10），毛玻璃质感一眼可辨；浮岛阴影同步加深。
- **底栏点击出现方块灰底**：导航项改用 `MutableInteractionSource` + `indication = null` 去除默认水波纹方块，点击/切换栏目不再有灰色方块闪烁。
- **「无更多直播间」判定错误**：
  - 根因一（误判还有更多）：斗鱼「语音互动-唱歌」等小栏目接口一页只返回 5 条、下一页返回 0 条，旧逻辑把「返回 5 条」当成还有更多。现已改为「返回条数 < 单页上限（斗鱼/虎牙/B站 20、抖音 15）即视为最后一页」。
  - 根因二（永不收敛）：条目不满一屏时，旧翻页触发器因 `distinctUntilChanged` 吞掉后续发射而永远不再触发，既加载不了也收敛不了。现已改为「列表未填满视口（underfilled）也触发加载」，并连续加载直到填满或条目数不再增长（带防死循环 guard）。
- **横屏滚动弹幕缓冲满后永久停更**：热门房一两分钟后缓冲区（200 条）即满，`takeLast` 让 `messages.size` 恒等于上限、差值恒为 0，滚动弹幕彻底停止上新。现改用独立自增 `revision` + `newCount` 作为「来了新弹幕」的唯一判据，缓冲满后新弹幕照常上屏。
- **切换直播间时旧地址覆盖新地址**：连点切换画质/线路或快速切房时，慢的旧解析结果会后到并覆写新直播间的播放地址，导致画面串台。现已在每次解析前取消上一个任务，并在赋值前校验「当前房间 == 请求时房间」，不符则丢弃旧结果。
- **竖排弹幕列表整列重绑**：弹幕 key 原含 index，缓冲 `takeLast` 导致所有条目 index 平移、key 全变、整列重组。现改为以消息对象身份做 key，新增一条只重组该条。
- **分类 JSON 主线程解析**：虎牙 / B站 / 抖音 的分类列表此前在主线程读取并解析 assets 中的 JSON（虎牙 66KB、B站 69KB），每次进平台页都会卡一下。现已整体挪到 `Dispatchers.IO`，进页更顺滑。

### 优化
- 依赖复查（Kotlin 2.0.21 / CMP 1.6.11 工具链上限下）：ktor 2.3.12、okhttp 4.12.0、media3 1.10.1、coil 2.7.0、coroutines 1.10.2、serialization-json 1.8.1、haze 0.7.3、zxing 3.5.4、rhino 1.7.14 等均为当前兼容的最新稳定版；网络栈与播放链路（ktor/okhttp/media3）大版本升级此前验证会引发虎牙/抖音黑屏，本次继续暂缓，优先保障播放稳定。

## v0.1.16

### 新特性
- **悬浮椭圆毛玻璃底栏**：底部「首页 / 虎牙 / 斗鱼 / 抖音 / B站」导航栏改为悬浮胶囊浮岛（参考 Telegram 风格），背景对滚动内容实时模糊（Android 12+ 真实毛玻璃，旧系统自动降级为半透明磨砂），浅色/深色模式分别使用偏白/偏黑的玻璃着色，并带描边与投影。
- **列表底部避让**：内容延伸至浮岛下方，关注页、各平台直播间列表、搜索结果的滚动区域底部均预留出浮岛高度——下滑到最后时直播间卡片可以完整显示，不会被底栏挡住。
- **「无更多直播间」提示**：平台栏目没有更多内容时，列表底部显示「无更多直播间」；只有还有更多内容时才显示「继续滑动加载更多」。
- **配色适配**：分区胶囊（CategoryPill）等表面元素同步改为半透明质感，与毛玻璃底栏在浅色/深色两种模式下保持统一的玻璃风格。

### 其他
- 依赖回归 v0.1.15 基线后仅新增 `haze` / `haze-materials` 0.7.3（毛玻璃库，兼容 Compose Multiplatform 1.6.x），其余依赖与 v0.1.15 完全一致。

## v0.1.15

### 修复问题
- **画中画「退出」后重进直播间功能按钮消失**：Activity 在画中画中被关闭时，系统不会再回调"退出画中画"，单例画中画状态会残留为 true，导致再次进入直播间时画中画 / 横屏等所有控件被误隐藏。现已在新建 Activity 时与「退出」按钮点击时主动复位画中画状态，彻底根治
- **画中画「退出」按钮观感优化**：图标改为高分辨率绘制——柔和半透明深色圆底 + 一圈极细高光描边 + 圆头白色 X，在亮/暗视频画面上都清晰干净，小尺寸下边缘不糊

### 优化
- 依赖升级（保守策略，均为同大版本内的稳定版）：kotlinx-coroutines 1.9.0 → 1.10.2（运行时性能与稳定性修复）、androidx.lifecycle 2.8.6 → 2.9.4（补丁线更新）。media3 1.11.0 / ktor 3.x / coil 3.x 等大版本升级经评估暂缓，优先保障播放与网络层稳定

## v0.1.14

### 修复问题
- **首页刷新图标与下拉刷新结果不一致**：顶栏「刷新」图标此前在 `refreshFollowedStreamerCards()` 之外还额外调用 `refreshFollowedLiveStatus()`，后者基于房间页 HTML / 独立在线接口判断直播状态，对「刚刚下播」的主播会误判为仍在线（例如虎牙房间页残留的 `stream` 数据块），表现为刷新图标刷出已下播却显示在线、而下拉刷新正常。现统一两条刷新路径都只走关注列表 API 的权威直播状态，结果完全一致
- **横屏全屏缺少退出按钮**：横屏全屏播放时，视频左上角新增「X」图标，点击即退出全屏回到竖屏（仍停留在播放器内），无需先点系统返回
- **画中画竖屏带入底部切换栏**：画中画在竖屏状态下会带上首页 / 虎牙 / 斗鱼等底部板块栏。现进入画中画前自动隐藏底部栏并让视频铺满，竖屏与横屏画中画画面统一为「只有视频」
- **画中画关闭未退出 App**：画中画窗口内新增「退出」按钮，点击直接关闭整个 App，并触发「设置 → 基本设置 → 退出时清理缓存」（登录态、设置、关注列表均保留）；行为确定，不受各厂商系统按钮差异影响

### 优化
- 依赖复查（Kotlin 2.0.21 工具链上限下）：coroutines 1.9.0 / serialization-json 1.8.1 / ktor 2.3.12 / okhttp 4.12.0 / coil 2.7.0 / media3 1.10.1 / core-ktx 1.15.0 / activity-compose 1.10.1 均为当前最新兼容版本；本次以稳定性为主，不做大版本升级，避免引入不兼容

## v0.1.13

### 新增功能
- 设置 → 基本设置 提供「主题模式」三档选择：**浅色 / 深色 / 跟随系统**
  - 浅色：沿用现有的浅色主题
  - 深色：完整适配的深色主题（冷色调深蓝黑底 + 高对比文字）
  - 跟随系统：读取系统深色模式设置自动切换，系统切换时即时生效
  - 选择会被持久化保存，重启 App 后保持

### 优化
- **深色主题配色统一**：此前深色模式下卡片、分类药丸、骨架屏用的是中性灰（`0xFF121212` / `0xFF1A1A1A` / `0xFF252525`），而页面背景是冷色调深蓝黑，导致「卡片发灰、背景发蓝」的割裂感。现已全部对齐到统一的冷色深色盘（背景 `0xFF0B0F14` / 卡片 `0xFF121720` / 卡片三级 `0xFF1A2230`），深色模式整体色调一致
- **弹幕批量合流（性能优化）**：此前每收到一条弹幕就执行一次 `列表 + 新消息` 再 `takeLast(200)`，即每条弹幕两次 O(n) 整表复制，且列表身份变更会触发整块弹幕面板重组。热门房间（每秒数十条）这是主要的卡顿与 GC 来源。现改为先攒批、再按节奏（攒满 24 条或每 120ms）一次性并入，复制与重组次数大幅下降；并配定时排水协程，保证最后一批不会滞留缓冲区，单条弹幕最多延迟 120ms 上屏
- 依赖升级（均通过 Maven 实测确认版本真实存在，且仍兼容 Kotlin 2.0.21 工具链）：
  - `androidx.core:core-ktx` 1.15.0 → **1.17.0**：1.17.0 是仍锁定 `kotlin-stdlib 2.0.21` 的最高稳定版（1.19.0 起要求 2.1.20，与本项目 Kotlin 2.0.21 不兼容）；升级后依赖图不再混入旧的 `kotlin-stdlib 1.8.22`
  - `com.google.zxing:core` 3.5.3 → **3.5.4**：扫码模块的补丁级修复
  - 未升级及原因：`kotlinx-coroutines`（1.10.x 要求 stdlib 2.1.0、1.11.0 要求 2.2.20）、`kotlinx-serialization`（1.9.0 要求 stdlib 2.2.0）均越过 Kotlin 2.0.21 上限；`ktor 3.x` / `okhttp 5.x` / `coil 3.x` 为破坏性大版本；`media3 1.11.0` 播放核心风险过高。详见发布说明

### 修复问题
- 修复底部平台栏未选中图标**对比度写反**：深色模式下用中灰 `0xFF6B7280`（在深底上几乎不可见），浅色模式反而用浅灰 `0xFF9CA3AF`（在浅底上偏淡）。现改为深色底用亮灰、浅色底用深灰，两种模式对比度均正常

### 文档
- README 重写：补充功能一览、主题模式、常见问题与自建步骤，完善关键词以提升搜索可见性

## v0.1.12

### 修复问题
- 播放器右侧 5 个控制按钮（设置 / 刷新 / 全屏 / 画中画 / 熄屏听播）在竖屏直播间中因视频区高度不足被裁切、只显示部分按钮：改为根据视频区可用高度**整体自适应缩放**整列按钮——竖屏视频区较矮时自动缩小按钮与间距，保证 5 个按钮全部可见；横屏视频区较高时保持原尺寸（40dp / 图标 24dp）。相比「按横竖屏写死两套尺寸」更鲁棒，对异形比例与小屏同样适用

### 优化
- 复查依赖（Kotlin 2.0.21 工具链上限下）：`androidx.media3`(ExoPlayer) 由 1.10.0 升至 **1.10.1**（1.10.x 补丁线，`kotlin-stdlib` 仍锁定 2.0.20，与当前 Kotlin 2.0.21 完全兼容，获取 ExoPlayer 缓冲 / 解码稳定性修复）
- 其余依赖（coroutines 1.9.0 / serialization-json 1.8.1 / ktor 2.3.12 / okhttp 4.12.0 / coil 2.7.0 / lifecycle 2.8.6 等）在 Kotlin 2.0.21 上限下均已是最新兼容版本，继续以稳定性为主，不做大版本升级

## v0.1.11

### 优化
- 升级播放器底层依赖 `androidx.media3`(ExoPlayer) 1.6.1 → **1.10.0**：获取 ExoPlayer 在缓冲调度、解码器适配与播放稳定性方面的累积修复，提升直播播放性能与稳定性
  - 说明：media3 ≥ 1.11.0 会强制要求 Kotlin 2.2 工具链（传递 `kotlin-stdlib` 2.2.x，与本项目锁定的 Kotlin 2.0.21 不兼容、编译直接失败），故在「不改动 Kotlin / Compose / AGP / Gradle 工具链、以稳定优先」的前提下，升到仍兼容 2.0.21 的最高版本 **1.10.0**（`kotlin-stdlib` 锁定 2.0.20）
- 其余依赖与工具链保持不变

## v0.1.10

### 新增功能
- 播放器新增「画中画」按钮（位于右侧控制栏倒数第二，耳机按钮之上）：点击后进入 Android 系统级画中画小窗，可边看直播边做别的事；进入小窗后自动隐藏所有控件与弹幕叠加层，只显示视频画面
- 平台板块（斗鱼 / 虎牙 / 抖音 / B站）顶部搜索框占位文案统一为「搜索」

### 优化
- 播放器右侧控制按钮尺寸调整（40dp，图标 24dp），点击区域更舒适
- 升级基础依赖以换取稳定性与性能收益：
  - `kotlinx-coroutines` 1.8.1 → 1.9.0
  - `kotlinx-serialization-json` 1.7.3 → 1.8.1
  - `androidx.core:core-ktx` 1.13.1 → 1.15.0
  - `androidx.activity:activity-compose` 1.9.2 → 1.10.1
  - `androidx.media3`(ExoPlayer) 1.3.1 → 1.6.1（播放器性能与稳定性）
  - `coil-compose` 2.6.0 → 2.7.0（图片加载）
  - 其余依赖（ktor / rhino / okhttp / quickjs / zxing 等）保持不动，以稳定为主

## v0.1.9

### 新增功能
- 设置 → 基本设置 新增「退出时清理缓存」开关（默认开启）：每次退出 App 自动清理直播缓存、临时文件等垃圾数据；登录状态（如 B站 Cookie）、设置、关注列表等均保留，不影响稳定性与流畅度

### 改名
- 仓库与 App 名称由 dtv_X 统一更名为 dtv_mx（图标保持不变）

### 修复问题
- 修复检查更新所指向的仓库名为旧名导致升级检测失效的问题（现已指向 dtv_mx）
- 修复 Release 说明抽取失败导致发布说明为空的问题：CHANGELOG 为 CRLF 换行时，CI 里按行精确匹配版本小节会失配；现已兼容 CRLF，并在说明为空时直接让构建失败
- 补齐 `lifecycle-process` 依赖，修复「退出时清理缓存」所需的进程级生命周期监听无法编译的问题

### 文档
- README 重写：补充下载安装、系统要求（Android 8.0+）、功能一览表、常见问题、自行构建步骤与项目说明，并完善搜索引擎可检索的关键词
- 新增 `.gitattributes`，统一仓库文本文件换行为 LF，避免 Windows 端 CRLF 混入仓库

## v0.1.8

### 修复问题
- 再次修复「默认横屏」下关闭直播间闪横屏：上次只覆盖 X 按钮，安卓系统手势返回（三键返回）在第一次返回变竖屏后再次返回时，会绕过播放器内部处理器、直接走上层关闭且未置关闭标记，仍会闪横屏。本次将返回处理器改为常驻拦截，所有退出路径（X 按钮 / 系统手势返回 / 顶部返回箭头）关闭前均先置 `isClosing`，关闭过程中即便 streamer 变化触发状态重新初始化成横屏，`FullscreenEffect` 也绝不再锁横屏，全程保持竖屏
- 首页顶栏「刷新」与「设置」图标间距进一步收紧：改用 `Box(36dp)+clickable` 取代默认 48dp 触摸尺寸的 `IconButton`，两图标视觉间隙从约 28dp 收到约 8dp

## v0.1.7

### 新增功能
- 首页顶栏「刷新」与「设置」图标归为一组（间距收紧），视觉上更紧凑

### 修复问题
- 彻底修复「默认横屏」下关闭直播间时**先横屏闪一下再恢复竖屏**的 BUG：新增关闭标记 `isClosing`，一旦开始关闭直播间，全屏方向控制立即禁止重新锁定横屏并强制回落竖屏，消除关闭过程中因 streamer 短暂变化触发状态重新初始化而闪现横屏的问题（X 按钮与返回键均生效）
- 各平台板块搜索框占位文案统一为「搜索」，避免长文案在窄屏换行

## v0.1.6

### 修复问题
- 修复「默认横屏」下点进直播间（横屏）后，点击右上角 X 关闭直播时会**先横屏闪一下再恢复竖屏**的 BUG：关闭直播间前先主动把方向切回竖屏（与系统返回键走同一套过渡逻辑），切屏淡出动画期间不再停留在横屏，退出后稳定保持竖屏

## v0.1.5

### 新增功能
- 首页顶栏（设置图标左侧）新增**手动刷新按钮**：点击后立即重新拉取关注主播的**直播快照（封面 / 标题 / 在线人数）与开播状态**，结果与下拉刷新一致；刷新过程中按钮转为加载指示器，避免重复触发

## v0.1.4

### 新增功能
- **新增「设置 → 平台设置」栏目**，包含两项能力：
  - **平台启用**：列出斗鱼、虎牙、抖音、B站四个平台，**默认全部开启**。关闭某个平台后，它会立即从底部切换栏移除；重新开启会加回原来的位置
  - **平台排序**：展示所有**已启用**的平台，**长按右侧手柄上下拖动**即可调整顺序，底部切换栏会立即按新顺序排列
  - 开关状态与排序结果都会持久保存，重启 App 后依然是当前设置；被关闭的平台重新启用时会回到它原来的相对位置

### 修复问题
- 修复开启「默认横屏」后，退出直播间 / 退出 App 时**系统界面（桌面、状态栏）也被带着横屏**的问题。现在横屏只作用于播放器内部，退出后 App 与系统界面都会回到正常的重力感应行为

### 体积优化
- 安装包体积从 **21.7 MB 降至约 18.5 MB**（约 -15%），**功能与稳定性完全不受影响**：
  - 单色启动图标此前把 1024×1024 的大图塞进了全部 5 个分辨率目录，已按各密度重新生成（约 -1.5 MB）
  - 只打包真机使用的 ARM 架构，移除手机上永远用不到的 x86 / x86_64 版 `libquickjs.so`（约 -1.7 MB）
  - 未开启 R8 代码压缩（此前开启会导致斗鱼直播间黑屏），稳定性优先

## v0.1.3

### 新增功能
- 开启熄屏听播时自动申请通知权限（Android 13+），「正在后台播放直播音频」常驻通知可正常显示在通知栏（未授权也不影响后台播放）
- 构建时自动校验签名密钥库完整可用，避免密钥异常产出无法升级的安装包

### 修复问题
- 修复签名密钥库被提交进公开仓库的安全问题：密钥已迁移到 GitHub Secrets，不再出现在代码库中。**使用的是同一把密钥，已安装的版本仍可正常 OTA 在线升级，不受任何影响**

## v0.1.2

### 新增功能
- **熄屏听播改为播放器内一键开关**：入口从「设置 → 基本设置」移到直播间右侧控制栏，位于原有 3 个图标（设置 / 刷新 / 全屏）**最下方**的耳机按钮
  - 点击耳机即开启：立即停止画面解码与渲染，只保留音频，省电省流量
  - 开启后图标变为「关闭耳机」并高亮为当前主题色，再次点击即可手动关闭
  - **每次退出直播间自动关闭**，不做持久化，每次进入都是默认关闭状态
  - 开启期间由前台服务保活，息屏 / 切到后台都不会被系统清理，声音持续播放
  - 通知栏常驻「正在后台播放直播音频」，点击可回到直播间

### 修复问题
- 修复开启熄屏听播后，弹幕仍在后台持续收发、不断刷新界面，导致后台占用偏高的问题（现在听播期间会断开弹幕连接，后台只剩一路音频解码）
- 修复播放器重建（如切换画质）后，纯音频约束可能漏生效、仍在渲染视频画面的问题
- 修复熄屏听播入口藏在设置里、看直播时无法随手开关的问题

## v0.1.1

### 新增功能
- **熄屏听播**（设置 → 基本设置，**默认关闭**）：开启后息屏或切到后台，直播声音继续播放，适合只听声不看画面的场景
  - 系统会保留应用不被后台清理，播放不会中断
  - 息屏 / 切后台后自动停止视频画面的解码与渲染，只保留音频，占用与耗电极低
  - 亮屏 / 回到前台自动恢复画面
  - 通知栏常驻一条「正在后台播放直播音频」的提示，点击即可回到直播间
- 开关状态会持久保存，重启 App 后仍保持当前设置

### 修复问题
- 修复息屏或切到后台后，直播被系统回收、声音立刻中断的问题
- 修复息屏后仍在解码并渲染视频画面，造成额外内存占用与耗电的问题

## v0.1

### 应用图标
- dTVX 艺术化设计图标：黑色背景 + dTVX 字母 2×2 紧凑排版（d、t 上排，v、x 下排）+ 玫瑰金到蓝紫双色渐变 + 后方金蓝光环弧线

### 多平台直播聚合
- 支持斗鱼、虎牙、抖音、B站四大平台直播观看
- 顶部分类（一级 / 二级 / 三级）自由切换，并自动记住上次所选栏目
- B站支持二维码登录与账号密码登录两种方式

### 首页与平台页布局
- 首页聚焦正在直播的内容，移除「置顶直播 / 置顶主播」板块，排版更紧凑、卡片不错位
- 每个平台板块顶部应用栏最右上角提供「订阅 / 已订阅」按钮，入口直观统一、与同行元素垂直居中对齐
- 移除冗余的「简易模式（九宫格）」切换入口

### 设置 · 基本设置
- **小卡片模式**（默认开启）：3 列紧凑卡片，一屏可展示约翻倍的直播间
- **默认横屏**（默认关闭）：开启后点进任意直播间即横屏全屏观看，更贴合大屏看直播体验；关闭则保持竖屏进入。开关持久保存，重启 App 后依然保持
- **画质档位**（默认最高）：最高（不限制分辨率 + 强制最高码率）/ 高（1080p）/ 中（720p）/ 低（480p），网络差可随时降档
- **记住栏目**（默认开启）：斗鱼 / 虎牙 / 抖音 / B站切换栏目自动记录，再次进入恢复上次选择
- **主题模式**：跟随系统 / 亮色 / 暗色
- **全局颜色**：13 种预设强调色，**默认青色**；点击「恢复默认颜色」即恢复为青色，应用于选中栏目高亮、弹幕昵称、顶栏标题与底部导航选中态

### 直播间画质
- 进房默认画质跟随「设置 · 基本设置 · 画质」全局档位，并按各平台规则映射为真实的最高 / 高 / 中 / 低档位
- 首选档位不可用时（未登录、非大会员、本场无该档位）自动逐级降级，并把实际生效档位回写到界面
- 画质选择仅对当前直播间生效，重开 App 以全局档位为准

### 性能与体验
- 列表滚动更顺滑：条目按类型复用，减少重组
- 图片加载更稳：封面按控件尺寸解码 + 淡入过渡，并补充图床所需的请求头，降低封面 / 头像加载失败率，同时降低内存占用
- 播放器：直播 / 点播分别配置缓冲策略（直播更快更实时、点播抗抖动）；接入音频焦点，来电或其他媒体播放时自动让出；离开播放页彻底释放解码器与底层资源
- 切换平台更流畅：底部导航不再在加载期间被锁定，进入任意平台都可随时切换

### 检查更新（应用内 OTA）
- 设置页一键查询仓库 Release，发现新版本自动下载并唤起安装
- 安装包缓存到系统「Download」目录：下载完成后若尚未授予安装权限，前往系统设置开启后返回，无需重新下载，直接再次安装即可；安装完成后自动清理缓存安装包
- 自动引导开启「允许安装未知应用」，慢网络下自动延长下载超时，保障大包完整下载

### 列表与交互
- 关注列表支持拖拽排序
- 底部加载提示仅在还有更多数据时显示，没有更多时不再占位；提示最下方居中独占一行，列数跟随小卡片模式

### B 站登录
- 登录面板支持二维码与账号密码两种方式，默认选中二维码
- 二维码登录面板直接展开到顶，扫码后自动完成登录
- 登录态（SESSDATA / bili_jct / DedeUserID 等）持久化保存，重启后依然保持登录

> 本项目仅用于学习与技术交流；播放内容与相关数据来自第三方平台接口，版权归属第三方。
